// 2) Escreva um programa que realize uma iteração entre 0 e 50 e determine se cada número é par ou ímpar.
for (let i = 0; i <= 50; i++) {
    console.log(i % 2 === 0 ? `${i} é par` : `${i} é ímpar`);
}
